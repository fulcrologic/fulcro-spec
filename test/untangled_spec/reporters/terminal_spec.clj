(ns untangled-spec.reporters.terminal-spec
  (:require [untangled-spec.core
             :refer [specification behavior provided assertions]]
            [untangled-spec.reporters.terminal]
            [clojure.test :as t]))

(specification "untangled-spec.reporters.terminal-spec"
  (behavior "TODO"
    (assertions
      )))
